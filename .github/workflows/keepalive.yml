name: Keep Supabase alive
on:
  schedule:
    - cron: "0 0 */3 * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sf -o /dev/null "https://nqeuarvpkxupxeeuzuow.supabase.co/rest/v1/" -H "apikey: ${{ secrets.SUPABASE_PUBLISHABLE_KEY }}" && echo "alive"
