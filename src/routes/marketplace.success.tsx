import { createFileRoute } from "@tanstack/react-router";
import { useServerFn } from "@tanstack/react-start";
import { useEffect, useState } from "react";
import { verifyCheckoutSession } from "@/lib/stripe-checkout.functions";
import { downloadAsset } from "@/lib/marketplace-download.functions";
export const Route = createFileRoute("/marketplace/success")({ validateSearch: (s) => ({ session_id: String(s.session_id ?? ""), listing_id: String(s.listing_id ?? "") }), head: () => ({ meta: [{ title: "Marketplace Success — Signhify | AI Product Studio" }, { name: "description", content: "Confirm your marketplace purchase and unlock the signed download link for your Signhify asset." }, { property: "og:url", content: "https://signhify.online/marketplace/success" }, { property: "og:title", content: "Marketplace Purchase Success" }], links: [{ rel: "canonical", href: "https://signhify.online/marketplace/success" }] }), component: SuccessPage });
function SuccessPage(){ const {session_id, listing_id}=Route.useSearch(); const verify=useServerFn(verifyCheckoutSession); const dl=useServerFn(downloadAsset); const [paid,setPaid]=useState(false); const [err,setErr]=useState(""); useEffect(()=>{verify({data:{sessionId:session_id}}).then(r=>setPaid(r.paid)).catch(e=>setErr(e.message))},[session_id]); return <section className="pt-36 pb-24 min-h-screen px-6"><div className="mx-auto max-w-2xl rounded-3xl border border-border bg-card p-8 text-center"><h1 className="font-display text-4xl font-black">Marketplace checkout</h1>{err&&<p className="mt-4 text-red-300">{err}</p>}{paid?<><div className="mt-6 rounded-xl border border-emerald-500/40 bg-emerald-500/10 p-4 text-emerald-300">Payment confirmed. Your download is unlocked.</div><button onClick={async()=>{const {signedUrl}=await dl({data:{listingId:listing_id}}); window.location.href=signedUrl;}} className="mt-6 rounded-md bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground">Download asset</button></>:<p className="mt-4 text-muted-foreground">Verifying payment…</p>}</div></section> }
